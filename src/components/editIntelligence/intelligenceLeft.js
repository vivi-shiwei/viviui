import React from 'react'
import {
  Box
} from '@chakra-ui/core'

const intelligenceLeft = ({ children, ...props }) => {
  return (
    <Box width='40%'>
      {children}
    </Box>
  )
}
export default intelligenceLeft
